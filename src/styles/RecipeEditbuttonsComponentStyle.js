import { StyleSheet } from "react-native"

export const RecipeEditbuttonsComponentStyle = () => {
    return (
        StyleSheet.create({
            container: {
                flex: 1,
                flexDirection: "row",
                justifyContent: "center",
            },
        })
    )
}